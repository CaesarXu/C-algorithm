#include"stdio.h"
int main(int argc,char * argv[]){
      int i,j,a[3];
      int b=0;
      for(i=0;i<3;i++){
      scanf("%d\n",&a[i]);}
      for (size_t (j) = 0; (j) < 3; (j)++) {

        b += a[j];
        /* code */printf("%d\n",a[j]);

      }
      printf("%d\n",b);
  }
